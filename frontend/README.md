# frontend/ — Console user interface

The CLI (`com.hospital.Main`) is the frontend of this project:
menu-driven console that talks to the backend services and never touches CSV files directly.

Run from repo root: `./run.sh` (or `run.bat` on Windows).
